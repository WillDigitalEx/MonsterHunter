const theme = {
    defaultDomain: window.location.protocol + '//' + window.location.host,
    defaultRed: "#C50009",
    darkRed: "#7D160F",
    mainColor: "#333333",
    subColor: "#FFFFFF",
}

export default theme;

